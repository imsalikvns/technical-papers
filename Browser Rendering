# How Does a Browser Renders HTML, CSS, JS to DOM? What Is the Mechanism Behind it?

The mechanism behind the pixalisation of HTML, CSS and JS codes is called **Critical Rendering Path (CRP)**. It's a process where browser collects codes from the HTML, CSS and JS files and merges them into the rendering tree. Although this process isn't necessarily always sequential, but the baseline implementation flows in that order.

Let's breakdown the the rendering (CRP) process into step for easy explanation. When the browser receives the HTML, CSS and JS files from the network, it process them into the following sequence:

## 1. HTML to DOM

### 1.1
The browser reads the raw bites from the HTML file and converts them into characters based on encoding method such as UTF-8.

### 1.2
It converts those characters into W3C standards tokens like `<head>`, `<body>` etc.

### 1.3
Thetokens are converted into object with their rules and properties.

### 1.4
Finally, the browser strcture these nodes into a tree hierarchy. This is our DOM.

## 2. CSS to CSSOM

### 2.1
While parsing the HTML file, the browser encounters the CSS file through `<link>` or `<style>` tag. It pauses the HTML parsing (or performs in parallel) and follows pretty much the same process as the DOM.

### 2.2
It goes like raw bites > characters > tokens > objects > CSSOM.

## 3. JS

### 3.1
When browser encounters `<script>` tag, it pause the HTML rendering and loads the script, and passes it to browser's JS engine (V8 in Chrom, SpiderMonkey in FireFox).

### 3.2
Since JS script can affect DOM and CSSOM, it is given priority to read the correct style and object model.

### 3.3
Once its done, browser resumes DOM construction where it left off.

## 4. Integeration

Once the DOM, CSSOM and JS are done procession, the browser turns everything into a rendering tree. The rendering tree only contains the nodes which finally have to be visible on the screen. For example, it excludes elements with `display:none` and includes elements with `display:hidden` properties.

## 5. Layout and Formatting

The browser calculates the size and position for each element according to the screen size.

## 6. Painting

The browsers uses graphic engine (like Skia in Chrom) to convert the elements into bitmap and shows the final frame on the screen.

# References

- Link: https://developer.mozilla.org/en-US/docs/Web/Performance/Guides/Critical_rendering_path
- Link: https://developer.chrome.com/docs/web-platform/blink
- Link: https://web.dev/learn/performance/understanding-the-critical-path
